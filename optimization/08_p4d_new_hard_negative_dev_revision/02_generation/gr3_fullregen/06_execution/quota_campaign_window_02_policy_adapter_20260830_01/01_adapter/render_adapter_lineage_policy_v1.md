# Render adapter lineage policy

`semantic_prompt_sha256` remains the semantic specification. `render_prompt_sha256` and `CODEX_SAFE_STAGED_CV_V1` are generation provenance. Existing 132 are `LEGACY_NO_ADAPTER`.
